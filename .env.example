FLASK_SECRET_KEY=replace-with-a-long-random-secret
REQUIRE_FIREBASE=1
SHOW_BACKEND_BADGE=1

# Prefer FIREBASE_CREDENTIALS in hosted environments.
# Keep serviceAccountKey.json local only; it is ignored by git.
# FIREBASE_CREDENTIALS={"type":"service_account", "...":"..."}
